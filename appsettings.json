{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",

  // Cadena de conexión a la base de datos SQL Server
  "ConnectionStrings": {
    // - Server: nombre del servidor SQL 
    // - Database: nombre de la base de datos 
    // - Trusted_Connection=True: se usa la autenticación de Windows (no usuario/contraseña)
    // - TrustServerCertificate=True: se permite confiar en el certificado del servidor.
    "PerfumeriaConnection": "Server=MSI\\SQLEXPRESS;Database=PERFUMERIA_ABLIZZ;Trusted_Connection=True;TrustServerCertificate=True;"
  }
}
